# Nginx configuration for load balancing between two servers
# This config sets up reverse proxies for both a webapp (on port 80) and Airflow (on port 8080)
# It distributes traffic between two servers in a VPN network (255.255.0.1 and 255.255.0.2)
# The configuration includes WebSocket support for both services

user nginx;
worker_processes auto;
error_log /var/log/nginx/error.log warn;
pid /var/run/nginx.pid;

events {
    worker_connections 1024;
}

http {
    include /etc/nginx/mime.types;
    default_type application/octet-stream;

    log_format main '$remote_addr - $remote_user [$time_local] "$request" '
                    '$status $body_bytes_sent "$http_referer" '
                    '"$http_user_agent" "$http_x_forwarded_for"';

    access_log /var/log/nginx/access.log main;
    sendfile on;
    keepalive_timeout 65;

    # TLS certificate, shared by all server blocks below.
    # Mount the cert + key into the container (see docker-compose.yaml nginx volumes).
    ssl_certificate /etc/nginx/certs/fullchain.pem;
    ssl_certificate_key /etc/nginx/certs/privkey.pem;
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_prefer_server_ciphers on;

    # Cache TLS sessions across connections so repeat clients can resume instead
    # of doing a full handshake every time. shared:SSL:10m = ~40k sessions.
    ssl_session_cache shared:SSL:10m;
    ssl_session_timeout 10m;

    # Define upstream servers for webapp
    upstream webapp_servers {
        # Load balance between multiple webapp instances on the multiple hosts
        # Using ip_hash for session affinity (sticky sessions)
        ip_hash;

        # Replace these with the actual IP addresses of your PCs within the VPN
        server 255.255.0.1:8501;  # PC 1,  instance 1
        server 255.255.0.1:8502;  # PC 1,  instance 2
        server 255.255.0.1:8503;  # PC 1,  instance 3
        server 255.255.0.2:8501;  # PC 2,  instance 1
        server 255.255.0.2:8502;  # PC 2,  instance 2
        server 255.255.0.2:8503;  # PC 2,  instance 3
    }

    # Define upstream servers for airflow webserver
    upstream airflow_webservers {
        # Replace these with the actual IP addresses of your PCs within the VPN
        server 255.255.0.1:8080;  # PC 1
        server 255.255.0.2:8080;  # PC 2
    }

    # Define upstream servers for MCP server
    upstream mcp_servers {
        # Replace these with the actual IP addresses of your PCs within the VPN
        server 255.255.0.1:8089;  # PC 1
        server 255.255.0.2:8089;  # PC 2
    }

    # Define upstream servers for REST API
    upstream rest_api_servers {
        server 255.255.0.1:8090;  # PC 1
        server 255.255.0.2:8090;  # PC 2
    }

    # Redirect plaintext HTTP to HTTPS.
    server {
        listen 80;
        server_name localhost;
        return 301 https://$host$request_uri;
    }

    server {
        listen 443 ssl;
        listen 8501 ssl; # for serving webapp on streamlit port
        server_name localhost;

        # HSTS: tell browsers to stick to HTTPS for one year. `always` makes nginx
        # add the header on error responses too. Note: includeSubDomains applies
        # the policy to every subdomain of the served host — drop it if any
        # subdomain still needs plain HTTP.
        add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;

        # Basic auth restricts access to people inside the group.
        # The htpasswd file is gitignored; generate it on the host with:
        #   htpasswd -cB misc/.htpasswd admin
        #   htpasswd -B  misc/.htpasswd kraken
        auth_basic "AlphaKraken";
        auth_basic_user_file /etc/nginx/.htpasswd;

        # Streamlit's built-in healthz endpoint is exempted from auth so the
        # monitoring service (and any other liveness probe) can poll it.
        location = /healthz {
            auth_basic off;
            proxy_pass http://webapp_servers/healthz;
        }

        location / {
            proxy_pass http://webapp_servers/;
            proxy_max_temp_file_size 0;
            proxy_set_header Host $host:$server_port;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_set_header X-Remote-User $remote_user;

            # websocket support
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection "upgrade";
        }
    }

    server {
        listen 8080 ssl;
        server_name localhost;

        add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;

        location / {
            proxy_pass http://airflow_webservers/;
            proxy_max_temp_file_size 0;
            proxy_set_header Host $host:$server_port;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;

            # websocket support
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection "upgrade";
        }
    }

    server {
        listen 8089 ssl;
        server_name localhost;

        add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;

        location / {
            proxy_pass http://mcp_servers/;
            proxy_max_temp_file_size 0;
            proxy_set_header Host $host:$server_port;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;

            # websocket support (for streamable-http transport)
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection "upgrade";
        }
    }

    server {
        listen 8090;
        server_name localhost;

        location / {
            proxy_pass http://rest_api_servers/;
            proxy_max_temp_file_size 0;
            proxy_set_header Host $host:$server_port;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }
    }
}
